{
  "name": "atmentions",
  "version": "0.1.2",
  "description": "Webmentions for the ATmosphere — cross-app atproto reactions for any page.",
  "type": "module",
  "license": "MIT",
  "exports": {
    ".": "./src/index.js",
    "./widget": "./src/widget.js"
  },
  "files": [
    "src",
    "dist"
  ],
  "scripts": {
    "test": "node --test",
    "build": "node scripts/build.js",
    "format": "prettier --write \"src/**/*.{js,css}\" \"test/**/*.js\"",
    "format:check": "prettier --check \"src/**/*.{js,css}\" \"test/**/*.js\""
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "prettier": "^3.8.4"
  }
}
